/**
 * Domain Module Barrel Export Template
 * 
 * Copy this template when creating a new domain module.
 * Replace 'xxx' with the domain name (e.g., 'venues', 'deals', 'contacts').
 * 
 * Structure:
 * - xxx.routes.ts    - Express route handlers (thin controllers)
 * - xxx.storage.ts   - Storage interface and implementation
 * - xxx.service.ts   - (Optional) Service layer for AI/MCP actions
 * - index.ts         - Barrel export (this file)
 */

// Route registration function
// export { registerXxxRoutes } from './xxx.routes';

// Storage interface and class
// export { type IXxxStorage, XxxStorage } from './xxx.storage';

// Optional: Service layer (for high-value domains with AI/MCP actions)
// export { XxxService } from './xxx.service';

// Re-export types if needed by other domains
// export type { XxxEntity, CreateXxxInput, UpdateXxxInput } from './xxx.types';
